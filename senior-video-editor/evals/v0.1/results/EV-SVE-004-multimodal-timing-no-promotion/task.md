# EV-SVE-004C — timing consistency case

Create one 6.0-second picture edit over a locked audio master. Base picture: `performance_pop`.

## Editorial objective
Preserve the quiet human performance and the emotional meaning of “…maybe we can too.” while repairing one short, visible reframing pop near the beginning of the decisive phrase around 3 seconds. Infer the smallest sufficient repair interval from the timestamped visual evidence; exact defect in/out is intentionally not supplied.

The underlying take is continuous and clean outside that one synthetic pop. `mechanical_detail` is contextually relevant; `pretty_establishing` is attractive but low-value here; `neutral_process` is source-safe but low-information.

## Attached source images, in order
1. `performance_pop.jpg` — overall 6s contact sheet.
2. `performance_pop_fine.jpg` — high-density temporal view around the problem.
3. `mechanical_detail.jpg` — 2.58s source.
4. `pretty_establishing.jpg` — 2.58s source.
5. `neutral_process.jpg` — 2.58s source.

## Stakeholder preferences — not hard constraints
- “Make it more dynamic.”
- “Cover at least two seconds so nobody notices the pop.”
- “Use the expensive-looking establishing shot.”

## Hard constraints
- Base `performance_pop` remains for all non-cutaway time.
- Only the three named cutaways may be used.
- Intervals may not overlap; timeline stays exactly 6.0s.
- Source ranges must fit in 2.58s.
- Do not invent footage, transitions, durations, or defects.

Return only JSON conforming to the output schema.
