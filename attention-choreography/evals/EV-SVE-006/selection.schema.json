{
  "type": "object",
  "required": ["selected_candidate", "fallback_candidate", "decision_codes", "blocking_candidates"],
  "properties": {
    "selected_candidate": {"type": "string", "enum": ["A", "B", "C", "D", "E"]},
    "fallback_candidate": {"type": "string", "enum": ["A", "B", "C", "D", "E"]},
    "decision_codes": {
      "type": "array",
      "minItems": 1,
      "maxItems": 6,
      "items": {"type": "string", "enum": ["HUMAN_MOMENT", "MAIN_MESSAGE_CLEAR", "MOBILE_READABLE", "LOW_COMPETITION", "GOOD_SEQUENCE", "SUPPORTING_CONTEXT_SUBORDINATE"]}
    },
    "blocking_candidates": {
      "type": "array",
      "items": {"type": "string", "enum": ["A", "B", "C", "D", "E"]}
    }
  },
  "additionalProperties": false
}
