interface:
  display_name: Supervising Video Editor
  short_description: Faz QA editorial independente com blockers antes de score
  default_prompt: Use $supervising-video-editor to independently judge this candidate against its VideoEditContract. Run blocking gates before weighted scoring, separate observable defects from taste, do not repair while judging, and mark unsupported specialist passes NOT_CERTIFIED. Return user-visible prose in Brazilian Portuguese.
policy:
  allow_implicit_invocation: true
  products: [chatgpt, codex, api, atlas]
