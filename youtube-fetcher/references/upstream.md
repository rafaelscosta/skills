# Upstream provenance

This skill is derived from and substantially extends:

- Repository: `JimmySadek/youtube-fetcher-to-markdown`
- Upstream license: MIT
- Original runtime purpose: fetch YouTube captions plus metadata and save an Obsidian-ready Markdown archive.

The local v2 adds agent-native selective retrieval, temporal/chapter selection,
deterministic transcript chunks, machine receipts, execution manifests, schemas,
GPT-5.6 routing/context discipline, and catalog-specific contracts/evals.

The upstream MIT notice is preserved in `LICENSE`.
